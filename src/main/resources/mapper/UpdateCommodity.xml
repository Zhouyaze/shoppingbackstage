<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//com.order.test.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhkj.shopmall.shoppingbackstage.shopping_backstage_dao.mapper.shopping_backstage_Commodity.UpdateCommodityMapper">

    <update id="UpdateCommodity">
        UPDATE  shoppingmall.commodity
        <set>
            <if test="commodityName!=null">commodityName =#{commodityName}</if>
            <if test="commodityName!=null">commodityName =#{commodityName}</if>
            <if test="commodityName!=null">commodityName =#{commodityName}</if>
        </set>
        WHERE id=#{id}
    </update>

    <update id="UpdateCommodityPice">
        UPDATE  shoppingmall.commodity_specification_inventory_price
        <set>
            <if test="specification1!=null">specification1=#{specification1}</if>
            <if test="specification2!=null">specification2=#{specification2}</if>
            <if test="specification3!=null">specification3=#{specification3} </if>
            <if test="specification4!=null">specification4=#{specification4} </if>
            <if test="inventory!=null">inventory=#{inventory} </if>
            <if test="price!=null">price=#{price} </if>
            <if test="picture!=null">price=#{picture} </if>
        </set>
        WHERE id=#{id}
    </update>

    <update id="UpdateCommodityPicture">
        UPDATE  shoppingmall.commodityintroducepicture
        <set>
          <if test="pictureUrl!=null"> pictureUrl=#{pictureUrl}</if>
          <if test="levels!=null"> levels=#{levels}</if>
        </set>
        WHERE id=#{id}
    </update>


</mapper>